name: "publish"

on:
  push:
    branches:
      - main

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest]

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev libayatana-appindicator3-dev librsvg2-dev

      - name: install frontend dependencies
        run: npm install

      - name: ensure fallback icon exists
        run: |
          mkdir -p src-tauri/icons
          curl -L https://raw.githubusercontent.com/tauri-apps/tauri/dev/app-icon.png -o src-tauri/icons/32x32.png
        shell: bash

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}
        with:
          tagName: v__VERSION__ # tauri-action replaces this with the app version
          releaseName: "Tú Anh Xinh Gái v__VERSION__"
          releaseBody: "Bản cài đặt Desktop chính thức cho Tú Anh Xinh Gái. Tải tệp .msi hoặc .exe bên dưới."
          releaseDraft: true
          prerelease: false
